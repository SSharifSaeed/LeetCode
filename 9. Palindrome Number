#include <stdio.h>
#include <stdbool.h>
bool isPalindrome(int x) {
    int rev_x = 0;
    if (x<0 || (x % 10 == 0 && x != 0))
        return false;
    while (x > rev_x){
        rev_x = rev_x *10 + x % 10;
        x /= 10;
    }

    return (x == rev_x || x == rev_x / 10);
}
